Read AGENTS.md, IMPLEMENTATION_PLAN.md, the active spec file, and progress/progress.txt.

`docs/architecture/Zeptalytic_Domain_Vocabulary_Decision_Record.md` is the canonical source for parent-site vocabulary, enum values, and status names.

Then:
- for planning runs, follow `prompt/prompt_plan.md`
- for build runs, follow `prompt/prompt_build.md`

The active workstream sequence is:
1. Discord identity schema correction
2. rewards domain DB schema
3. rewards verification / regression
4. rewards API / application layer